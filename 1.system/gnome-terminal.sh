#!/usr/bin/env bash

# if konsole can not type not chinese, install this replace it

sudo apt-get install gnome-terminal -y
sudo apt-get install dconf-cli
echo "run 'gnome-terminal' open gnome-terminal"